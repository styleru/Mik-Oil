<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/login_fragment_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/name"
        android:hint="@string/name"
        android:importantForAutofill="no"
        android:inputType="text"
        android:paddingStart="17dp"
        android:paddingTop="11dp"
        android:paddingEnd="17dp"
        android:paddingBottom="10dp"
        app:layout_constraintBottom_toTopOf="@id/telephone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.2"
        app:layout_constraintVertical_chainStyle="packed"
        style="@style/EditTextStyle"
        android:background="@drawable/button"/>


    <EditText
        android:id="@+id/telephone"

        android:layout_marginTop="22dp"
        android:background="@drawable/button"
        android:hint="@string/telephone_number"
        android:importantForAutofill="no"
        android:inputType="phone"
        android:paddingStart="17dp"
        android:paddingTop="11dp"
        android:paddingEnd="17dp"
        android:paddingBottom="10dp"
        app:layout_constraintBottom_toBottomOf="@id/password"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/name"
        style="@style/EditTextStyle"/>

    <EditText
        android:id="@+id/password"
        android:layout_marginTop="22dp"
        android:background="@drawable/button"
        android:hint="@string/password"
        android:importantForAutofill="no"
        android:inputType="textPassword"
        android:paddingStart="17dp"
        android:paddingTop="11dp"
        android:paddingEnd="17dp"
        android:paddingBottom="10dp"
        app:layout_constraintBottom_toBottomOf="@id/repeat_password"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/telephone"
        style="@style/EditTextStyle"/>

    <EditText
        android:id="@+id/repeat_password"

        android:layout_marginTop="22dp"
        android:background="@drawable/button"
        android:hint="@string/repeat_password"
        android:importantForAutofill="no"
        android:inputType="textPassword"
        android:paddingStart="17dp"
        android:paddingTop="11dp"
        android:paddingEnd="17dp"
        android:paddingBottom="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/password"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.0"
        style="@style/EditTextStyle"
        />

    <Button
        android:id="@+id/go_registration"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:background="@drawable/button"
        android:elevation="7dp"
        android:stateListAnimator="@drawable/button_animator"
        android:text="@string/go"
        android:textSize="27sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/repeat_password"
        app:layout_constraintVertical_bias="0.7" />

    <ProgressBar
        android:id="@+id/progress_registration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/go_registration"
        app:layout_constraintEnd_toEndOf="@+id/go_registration"
        app:layout_constraintStart_toStartOf="@+id/go_registration"
        app:layout_constraintTop_toTopOf="@+id/go_registration" />

</androidx.constraintlayout.widget.ConstraintLayout>